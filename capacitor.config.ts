import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gh-issue-390',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
